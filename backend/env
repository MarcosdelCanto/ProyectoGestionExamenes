# Clave secreta para firmar los JWT
JWT_SECRET=tu_clave_secreta_super_segura
# Tiempo de expiración (opcional, p.ej. 1h, 2d)
JWT_EXPIRES_IN=3h
# URL base de la API (opcional, p.ej. http://localhost:3000)
API_BASE_URL=http://localhost:3000
PORT=3000
# Configuración de CORS (opcional, p.ej. http://localhost:3000)
CORS_ORIGIN=http://localhost:3000
# Configuración de la base de datos
DB_USER=admin
DB_PASSWORD=Salasasig136.
DB_CONNECTSTRING=basedatosexamenes_low
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_INCREMENT=2
TNS_ADMIN=.\wallet
